# Copy this file to .env and fill in any keys you want to use.
# You can run the app WITHOUT any keys (it will use an offline rule-based generator).
# If you add keys, the app will prefer OpenAI; otherwise it will try Hugging Face Inference API.

# OpenAI (paid)
OPENAI_API_KEY=

# Hugging Face Inference API (free tier available)
HF_API_TOKEN=

# Default models (you can change these)
OPENAI_MODEL=gpt-4o-mini
HF_MODEL=mistralai/Mistral-7B-Instruct-v0.2
